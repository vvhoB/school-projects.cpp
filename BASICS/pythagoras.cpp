#include<iostream>
#include<cmath>
using namespace std;

int main ()
{
    float a, b ;
    cout << "Enter a: ";
    cin >> a;
    cout << "Enter b: ";
    cin >> b;
    cout << "c = " << sqrt(a*a+b*b);
}

//The program calculates the length of the hypotenuse c 
//of a right triangle a,b,c using the pythagoras theorem
